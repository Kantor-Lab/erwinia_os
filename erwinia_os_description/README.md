# Erwinia OS Description

Holds the URDF, SRDF, meshes, RViz configurations, and launch files for the Erwinia robot platform. Populate the `urdf/` and `xacro` files first; other packages can then consume the generated `robot_description`.
