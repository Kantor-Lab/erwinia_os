# Erwinia OS Control

Scaffolding for ros2_control configuration and launch files for Erwinia. Fill `config/` with controller YAML (e.g., joint state broadcaster, trajectory controllers) and add launch files under `launch/` to start `ros2_control_node` and spawners.
